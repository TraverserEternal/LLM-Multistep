export type Theme = {
  primaryColor: string;
  secondaryColor: string;
  textColor: string;
};

export const defaultTheme: Theme = {
  primaryColor: "black",
  secondaryColor: "blue",
  textColor: "white",
};

export const secondTheme: Theme = {
  primaryColor: "blue",
  secondaryColor: "blue",
  textColor: "white",
};
